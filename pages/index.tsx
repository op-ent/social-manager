import type { NextPage } from "next";
import React from "react";
import LoginButton from "~/components/LoginButton";

const Home: NextPage = () => {
  return (
    <div>
      <LoginButton />
    </div>
  );
};

export default Home;
